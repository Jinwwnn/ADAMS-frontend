# RAG-LLM-Metric Environment Variables Configuration
# Copy this file to .env and fill in your API keys

# OpenAI API Key (Required) - Used for LLM evaluation and agent discussion
OPENAI_API_KEY=your_openai_api_key_here

# Hugging Face Token (Required) - Used for dataset read/write
HF_TOKEN=your_huggingface_token_here

# Answer Type (Required) - Specifies which column contains the answer for evaluation
# Common values: 'gold', 'response', 'Model_Answer', 'generated_answer'
# This helps the system identify which column to use as the answer in your dataset
ANSWER_TYPE=gold

# Usage Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Edit .env file and fill in your real API keys
# 3. Adjust ANSWER_TYPE based on your dataset column names
# 4. Make sure .env file is not committed to git repository (already excluded in .gitignore)
